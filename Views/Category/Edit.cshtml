@model Inventory.Models.CategoryModel
@{
    ViewData["Title"] = "Edit Category";
    Layout = "_Layout";
}

<div class="page-hero">
    <div>
        <h1>Edit category</h1>
        <p>Update “@Model.Category_Type” and its availability.</p>
    </div>
    <a asp-action="Index" class="btn btn-fx-outline"><i class="fas fa-arrow-left mr-2"></i>Back to categories</a>
</div>

<div class="fx-form-shell">
    <div class="card fx-card fx-form-card">
        <div class="card-header">
            <span class="fx-form-icon"><i class="fas fa-pen"></i></span>
            <div><strong>Category details</strong><div class="small text-muted">Category ID #@Model.Category_Id</div></div>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Category_Id">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Category_Type">Category name <span class="text-danger">*</span></label>
                    <input asp-for="Category_Type" class="form-control" maxlength="100" autofocus>
                    <span asp-validation-for="Category_Type" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Category_Description">Description</label>
                    <textarea asp-for="Category_Description" class="form-control" rows="4" maxlength="500"></textarea>
                    <div class="d-flex justify-content-between">
                        <span asp-validation-for="Category_Description" class="text-danger"></span>
                        <span class="fx-field-hint"><span id="descriptionCount">0</span>/500</span>
                    </div>
                </div>
                <div class="form-group mb-0">
                    <label asp-for="Category_Status">Status</label>
                    <select asp-for="Category_Status" class="form-control">
                        <option value="Active">Active — available for use</option>
                        <option value="Inactive">Inactive — hidden from selection</option>
                    </select>
                </div>
                <div class="fx-form-actions">
                    <a asp-action="Index" class="btn btn-fx-outline">Cancel</a>
                    <button type="submit" class="btn btn-fx"><i class="fas fa-save mr-2"></i>Save changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
    const description = document.getElementById('Category_Description');
    const counter = document.getElementById('descriptionCount');
    function updateCount() { counter.textContent = description.value.length; }
    description.addEventListener('input', updateCount);
    updateCount();
</script>
}
